var array = [12, 12, 14, 3, 32, 34, 343, 43, 54, 56, 72, 73, 87, 98];
var bestcase = array[array.length / 2];
var worstcase = array[array.length-1];
var averagecase = array[Math.floor(Math.random() * array.length) + 1];

function binarySearch(array, element){
    
}

binarySearch(array, bestcase);
binarySearch(array, worstcase);
binarySearch(array, averagecase);
